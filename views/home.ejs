<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu & Home</title>
    <link rel="stylesheet" type="text/css" href="/css/main.css" />
</head>
<body>
    <div class="container">
        <h1>Stock Market</h1>
        <ul class="menu">
            <li><a href="/views/about">About</a></li>
            <li><a href="/views/stocks">Stocks</a></li>
            <% if (typeof userId === 'undefined') { %>
                <li><a href="/users/login">Login</a></li>
                <li><a href="/users/register">Register</a></li>
            <% } else { %>
                <li><a href="/users/logout">Logout</a></li>
            <% } %>
        </ul>

        <!-- Conditional content based on login status -->
        <% if (typeof firstName !== 'undefined') { %>
            <h2>Welcome, <%= firstName %>!</h2>
            <p>You're logged in. Explore the stock market trends and more.</p>
        <% } else { %>
            <h2>Welcome to the Stocks Application</h2>
            <p>Please log in or register to access all features.</p>
        <% } %>
    </div>
</body>
</html>
